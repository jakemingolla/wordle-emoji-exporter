{
  "name": "wordle-emoji-exporter",
  "version": "1.1.0",
  "description": "Export your wordle results as emojis!",
  "main": "src/index.js",
  "scripts": {
    "build": "npm-run-all build:*",
    "build:lint": "npx prettier . --check",
    "build:generate-manifest": "node scripts/generate-manifest.js",
    "publish": "npm run build && node scripts/publish.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "wordle"
  ],
  "author": "Jake Mingolla",
  "license": "ISC",
  "devDependencies": {
    "archiver": "^5.3.0",
    "npm-run-all": "^4.1.5",
    "prettier": "^2.5.1"
  }
}
